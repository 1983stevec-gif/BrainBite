# Firebase Setup v10

BrainBite now uses Firebase as the only planned cloud backend.

Included:
- browser REST Auth for Email/Password
- Firestore REST profile push/pull
- local-first queue
- conflict-aware profile merge
- Firestore Security Rules
- config validation
- release tests

Not included:
- Admin SDK credentials in the browser
- server-side OCR secrets
