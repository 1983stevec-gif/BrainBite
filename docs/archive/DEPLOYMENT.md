# Deployment
Static PWA over HTTPS. Before beta run `npm install`, `npm run check:content`, `npx playwright install`, `npm run test:e2e`. Manually verify touch, keyboard, offline reload, profile separation, export/import, reduced motion, and high contrast.
