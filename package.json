{
  "name": "brainbite",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test:unit": "node --test tests/core.test.mjs",
    "test:e2e": "playwright test",
    "serve": "node scripts/serve.mjs",
    "check:content": "node scripts/validate-content.mjs",
    "beta:check": "npm run check:content && npm run test:unit && npm run test:e2e",
    "rc:check": "npm run check:content && npm run test:unit && npm run test:e2e",
    "release:check": "npm run check:content && npm run check:runtime && npm run check:release && npm run check:firebase && npm run check:launch && npm run check:final && npm run check:firebase:security && npm run check:evidence && npm run test:unit && npm run test:e2e",
    "check:runtime": "node scripts/validate-runtime.mjs",
    "check:release": "node scripts/validate-release.mjs",
    "check:firebase": "node scripts/validate-firebase.mjs",
    "check:launch": "node scripts/validate-launch.mjs",
    "check:final": "node scripts/validate-final.mjs",
    "check:firebase:security": "node scripts/validate-firebase-security.mjs",
    "check:evidence": "node scripts/validate-evidence.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.55.0"
  },
  "dependencies": {
    "three": "^0.170.0"
  }
}
